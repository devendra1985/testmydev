# Server Configuration
server.port=8087
server.servlet.context-path=/testmydev
server.shutdown=graceful

# Application Configuration
spring.application.name=testmydev

# ============================================================
# MySQL Database Configuration
# Defaults to localhost for local dev; override via env vars in Kubernetes
# ============================================================
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/inventory_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:rootpassword}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Initialize database with schema.sql and data.sql
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true

# Actuator Configuration
management.endpoints.web.exposure.include=health,info,metrics,prometheus,web
management.endpoints.web.discovery.enabled=true
management.endpoint.health.show-details=always
management.metrics.enable.jvm=true
management.metrics.enable.process.cpu=true
management.metrics.enable.system.cpu=true

# Logging Configuration
logging.level.com.example.testmydev=DEBUG
logging.level.org.springframework.web=INFO
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n

# Log all incoming HTTP requests (method, URI, status, duration)
logging.level.org.springframework.web.filter.CommonsRequestLoggingFilter=DEBUG

# ============================================================
# SAML 2.0 / Keycloak Configuration
# Override KEYCLOAK_HOST and KEYCLOAK_REALM via environment variables in production.
# ============================================================
spring.security.saml2.relyingparty.registration.keycloak.entity-id=http://localhost:8087/testmydev
spring.security.saml2.relyingparty.registration.keycloak.assertingparty.metadata-uri=${KEYCLOAK_HOST:http://localhost:8080}/realms/${KEYCLOAK_REALM:myrealm}/protocol/saml/descriptor
spring.security.saml2.relyingparty.registration.keycloak.acs.location=http://localhost:8087/testmydev/login/saml2/sso/keycloak
spring.security.saml2.relyingparty.registration.keycloak.singlelogout.url=http://localhost:8087/testmydev/logout/saml2/slo
spring.security.saml2.relyingparty.registration.keycloak.singlelogout.response-url=http://localhost:8087/testmydev/logout/saml2/slo

logging.level.org.springframework.security.saml2=DEBUG
